name: utility-safety-ai
channels:
  - defaults
dependencies:
  # Python 3.11 is the reference runtime used by the release workflow.
  - python=3.11
  - pip>=24
  - pip:
      # Run conda from the repository root; pyproject.toml owns all dependency ranges.
      - -r requirements.txt
