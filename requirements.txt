# Python 3.10–3.12 are supported and tested. Python 3.13 is not yet supported
# because numpy 1.x does not provide wheels for it.

# Core inference and image processing
ultralytics>=8.3.0,<9.0
lap>=0.5.12,<0.6
opencv-python>=4.8.0,<5.0
numpy>=1.26.0,<2.0.0
Pillow>=10.0.0,<13.0

# Web demo and CLI
streamlit>=1.38.0,<2.0
pyyaml>=6.0.1,<7.0
click>=8.1.0,<9.0
pandas>=2.0.0,<4.0

# Development and testing
build>=1.2.0,<2.0
mypy>=1.11.0,<2.0
pytest>=8.0.0,<10.0
pytest-cov>=5.0.0,<8.0
ruff>=0.6.0,<1.0
types-PyYAML>=6.0.12
